# Server-side only — used by api/plan.ts (the Vercel function). Never exposed to the browser.
ANTHROPIC_API_KEY=sk-ant-...

# Frontend build flag. Set to "false" to call the live /api/plan endpoint.
# Omit (or any other value) to keep the app fully seeded and offline-safe.
VITE_DEMO_MODE=false
